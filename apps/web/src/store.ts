import {create} from "zustand"; import {persist} from "zustand/middleware";
type State={onboarded:boolean;goal:string;minutes:number;xp:number;streak:number;completed:string[];firstBossComplete:boolean;finishOnboarding:(goal:string,minutes:number)=>void;complete:(level:string,xp:number)=>void;markFirstBossComplete:()=>void};
export const usePlayer=create<State>()(persist(set=>({onboarded:false,goal:"文化通识积累",minutes:15,xp:0,streak:0,completed:[],firstBossComplete:false,finishOnboarding:(goal,minutes)=>set({onboarded:true,goal,minutes}),complete:(level,xp)=>set(s=>({completed:[...new Set([...s.completed,level])],xp:s.xp+xp})),markFirstBossComplete:()=>set({firstBossComplete:true})}),{name:"knowledge-expedition-player"}));
