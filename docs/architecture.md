# 架构

浏览器只负责界面与临时状态。Fastify API 是安全边界，负责身份校验、判题、奖励、复习计划、订单和内容发布。Notion 是编辑源；已审核内容经同步适配器写入 Supabase。Supabase 保存内容镜像、Auth 和全部玩家数据。API 与外部服务之间均使用接口隔离，开发环境可使用 Mock。

题目公开响应由服务端白名单结构生成，不包含 `correct_answer` 与完整解析。生产环境禁止浏览器直接查询题目表。
